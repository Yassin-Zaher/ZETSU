import { BlockEvendDirective } from './block-evend.directive';

describe('BlockEvendDirective', () => {
  it('should create an instance', () => {
    const directive = new BlockEvendDirective();
    expect(directive).toBeTruthy();
  });
});
